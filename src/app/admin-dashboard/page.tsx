import { PageShell } from '@/components/PageShell';

export default function Page() {
  return (
    <PageShell title="Admin Dashboard">
      <p>Dashboard placeholder for bookings, drivers, and quote requests.</p>
    </PageShell>
  );
}
