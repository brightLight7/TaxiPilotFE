import { AutocompletecdDirective } from './autocompletecd.directive';

describe('AutocompletecdDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompletecdDirective();
    expect(directive).toBeTruthy();
  });
});
